# OpenRisk 정확도 향상을 위한 데이터 전략

> 작성일: 2025-12-30
> 목적: 신뢰도(=현실에서 안 틀리는 정도) 향상을 위한 공공데이터 활용 가이드

---

## 핵심 인사이트

현재 3개 지표(유동/시간대편차/주말비중)만으로 "상권 분위기"는 잡히지만, **신뢰도**를 올리려면 4가지 축이 더 필요:

| 축 | 현재 | 필요한 데이터 | 효과 |
|----|:----:|--------------|------|
| **공급(경쟁)** | ❌ | 점포-상권 | 같은 C라도 "빡센 C" vs "틈새 C" 구분 |
| **생존(교체율)** | ❌ | 상권변화지표 | 폐업 리스크 직접 감지 |
| **돈(매출)** | ❌ | 추정매출-상권 | 유동 많아도 매출 안 나오는 상권 감지 |
| **유입원(앵커)** | ❌ | 집객시설-상권 | 역/대학/병원 의존도 해석 보정 |

---

## 1. 현재 보유 데이터 현황

### 이미 보유한 공공데이터 (공공데이터 폴더)

| 파일 | 크기 | 현재 사용 | 활용 가능 지표 |
|------|------|:--------:|---------------|
| `trade_areas.geojson` | 10.5MB | ✅ | 상권 폴리곤, 중심좌표 |
| `traffic_utf8.csv` | 11MB | ✅ | 유동인구 (시간대/요일별) |
| `sales_utf8.csv` | 72MB | ❌ | 매출 → `cost_proxy` |
| `서울시 상권분석서비스(추정매출-상권).csv` | 70MB | ❌ | 업종별 상세 매출 |
| `상권배후지/상주인구.csv` | 5.5MB | ❌ | `resident_index` |
| `상권배후지/직장인구.csv` | 5MB | ❌ | `worker_index` |

---

## 2. MVP 데이터 우선순위 (80/20 원칙)

### 🔥 1순위: 점포-상권 (경쟁/공급)

**출처**: [서울시 상권분석서비스(점포-상권)](https://data.seoul.go.kr/dataList/OA-15577/S/1/datasetView.do)

**왜 가장 중요한가**:
> 지금 로직은 "사람 많다/적다"만 보는데, **경쟁자가 몇 명인지**는 못 봄
> → 같은 C등급이라도 "빡센 C" vs "틈새 C"가 갈림

**제공 컬럼 (예상)**:
| 컬럼 | 설명 | 파생 지표 |
|------|------|----------|
| `점포_수` | 전체 점포 | 기초 데이터 |
| `유사_업종_점포_수` | 동종업종 | `competition_density` |
| `개업_점포_수` / `폐업_점포_수` | 개폐업 | `open_close_churn` |
| `프랜차이즈_점포_수` | 프차 비중 | `franchise_ratio` |

**추천 파생지표**:
```
competition_density = 유사업종_점포수 / 전체_점포수
open_close_churn = (개업 + 폐업) / 전체_점포수
franchise_ratio = 프랜차이즈_점포수 / 전체_점포수
```

---

### 🔥 2순위: 상권변화지표-상권 (생존/교체율)

**출처**: [서울시 상권분석서비스(상권변화지표-상권)](https://data.seoul.go.kr/dataList/OA-15576/S/1/datasetView.do)

**왜 중요한가**:
> "초보자 무덤(C등급)"의 핵심은 **변화속도/교체율**인데, 이 데이터가 직접 잡아줌

**제공하는 4등급 변화지표**:

| 등급 | 의미 | 해석 |
|------|------|------|
| **LL** | 생존↓ 폐업↓ | 신규 개발/도시재생 상권, 주의 필요 |
| **LH** | 생존↓ 폐업↑ | **신규 업체가 경쟁력** 있는 상권 |
| **HL** | 생존↑ 폐업↓ | **기존 업체가 경쟁력** 있는 상권 (진입 장벽) |
| **HH** | 생존↑ 폐업↑ | 오래 버티면 좋지만 진입 주의 |

**활용**:
- `HL` + C등급 → "기존 단골 상권, 신규 진입 어려움" 경고
- `LH` + C등급 → "새로운 업체도 기회 있음" 해석

---

### 🔥 3순위: 추정매출-상권 (돈의 크기)

**출처**: [서울시 상권분석서비스(추정매출-상권)](https://data.seoul.go.kr/dataList/OA-15572/S/1/datasetView.do)
**(이미 보유: `sales_utf8.csv`, `추정매출-상권.csv`)**

**왜 중요한가**:
> 유동이 많아도 매출이 안 나오는 상권 있고, 유동 적어도 단가 높은 상권 있음

**제공 컬럼**:
| 컬럼 | 설명 | 파생 지표 |
|------|------|----------|
| `당월_매출_금액` | 월 총매출 | 상권 활성도 |
| `당월_매출_건수` | 결제 건수 | 객단가 추정 |
| `서비스_업종_코드` | 업종 | 업종별 분석 |
| `주중/주말_매출` | 요일별 | 주말 의존도 검증 |

**추천 파생지표**:
```
sales_per_store = 추정매출 / 점포수  (체감 난이도)
cost_proxy = 상권_평균매출 / 서울_전체_평균매출  (비용 압박 추정)
weekend_sales_ratio = 주말매출 / 전체매출  (관광/나들이형 신호)
```

---

### ⭐ 4순위: 상주인구/직장인구 (수요 성격)

**출처**:
- [상주인구-상권배후지](https://data.seoul.go.kr/dataList/OA-15569/S/1/datasetView.do)
- [직장인구-상권배후지](https://data.seoul.go.kr/dataList/OA-15570/S/1/datasetView.do)

**(이미 보유: `상권배후지/상주인구.csv`, `직장인구.csv`)**

**추천 파생지표**:
```
resident_worker_mix = resident / (resident + worker)
→ 0.7 이상: 주거형(A등급 신호)
→ 0.3 이하: 오피스형(평일 점심 편향)
→ 0.3~0.7: 혼합형(B등급 신호)

commuter_dependence = worker / traffic_total
→ 높으면 평일 점심 시간대 집중
```

---

### ⭐ 5순위: 집객시설-상권 (앵커 신호)

**출처**: [서울시 상권분석서비스(집객시설-상권)](https://data.seoul.go.kr/dataList/OA-15580/S/1/datasetView.do)

**제공 정보**: 학교/병원/역세권/관공서/문화시설 분포

**중요**: 앵커는 '유망'이 아니라 '유입 원인'

| 앵커 | 효과 | 리스크 |
|------|------|--------|
| 지하철역 | 유입↑ | 임대료↑, 경쟁↑ |
| 대학교 | 젊은층 | 방학 시즌성 |
| 병원 | 안정 수요 | 특정 업종만 유리 |
| 관광지 | 외부 유입 | 시즌/트렌드 의존 |

**활용**: 등급 뒤집기(X), **해석 보정(O)**

---

## 3. "광고 탄성" 신호 설계

GPT가 제안한 "광고가 C에서 먹히고 A에선 안 먹힌다"를 데이터로 반영:

### 마케팅 탄성 지표 (Marketing Elasticity Index)

```typescript
interface MarketingElasticity {
  level: 'HIGH' | 'MEDIUM' | 'LOW'
  description: string
}

function calculateMarketingElasticity(metrics: {
  nighttime_ratio: number      // 야간 유동 비율 (길단위인구)
  weekend_ratio: number        // 주말 비중
  external_inflow: boolean     // 집객시설(역/관광) 존재
  competition_density: number  // 경쟁 밀도
  resident_ratio: number       // 상주인구 비율
}): MarketingElasticity {

  // 야간/외부유입 높음 + 경쟁 높음 = 마케팅 영향 큼
  if (metrics.nighttime_ratio > 0.3 && metrics.external_inflow && metrics.competition_density > 0.5) {
    return {
      level: 'HIGH',
      description: '블로그/인스타/플레이스 영향 큼 (성과도↑, 변동성도↑)'
    }
  }

  // 상주 비중 높음 = 마케팅보다 단골/생활동선 최적화
  if (metrics.resident_ratio > 0.7) {
    return {
      level: 'LOW',
      description: '광고로 급반등 기대↓, 단골/생활동선 최적화가 핵심'
    }
  }

  return {
    level: 'MEDIUM',
    description: '적절한 마케팅과 운영 품질 균형 필요'
  }
}
```

---

## 4. 소득/소비 데이터 주의사항

> ⚠️ 서울 상권분석서비스의 `소득소비-상권배후지`는 **"행정동보다 작은 상권크기 데이터 제공이 어려워 더 이상 갱신되지 않는다"**고 명시

**결론**: 소득/소비는 MVP에선 제외, 필요시 행정동/자치구 단위 보조설명으로만 활용

---

## 5. DB 스키마 확장 설계

### 옵션 A: 기존 area_metrics 컬럼 확장 (추천 - MVP)

```sql
-- 기존 area_metrics에 컬럼 추가
ALTER TABLE area_metrics ADD COLUMN IF NOT EXISTS
  -- 점포/경쟁 지표
  store_count INTEGER,
  competition_density REAL,        -- 유사업종/전체
  franchise_ratio REAL,            -- 프차 비중

  -- 생존/변화 지표
  change_indicator TEXT,           -- 'LL', 'LH', 'HL', 'HH'
  survival_months_avg REAL,        -- 평균 생존 기간

  -- 매출 지표
  sales_total BIGINT,              -- 월 총매출
  sales_per_store REAL,            -- 점포당 매출

  -- 집객시설
  has_subway BOOLEAN,
  has_university BOOLEAN,
  has_hospital BOOLEAN,
  has_tourist_spot BOOLEAN;
```

**장점**: 단순, 기존 쿼리 수정 최소화
**단점**: 컬럼 많아짐

---

### 옵션 B: 테이블 분리 (V1 확장용)

```sql
-- 점포/경쟁 지표
CREATE TABLE store_metrics (
  id SERIAL PRIMARY KEY,
  area_id TEXT REFERENCES trade_areas(id),
  period TEXT NOT NULL,
  store_count INTEGER,
  similar_store_count INTEGER,
  franchise_count INTEGER,
  open_count INTEGER,
  close_count INTEGER,
  UNIQUE(area_id, period)
);

-- 매출 지표
CREATE TABLE sales_metrics (
  id SERIAL PRIMARY KEY,
  area_id TEXT REFERENCES trade_areas(id),
  period TEXT NOT NULL,
  industry_code TEXT,              -- 업종별
  sales_amount BIGINT,
  sales_count INTEGER,
  weekday_sales BIGINT,
  weekend_sales BIGINT,
  UNIQUE(area_id, period, industry_code)
);

-- 변화 지표
CREATE TABLE change_metrics (
  id SERIAL PRIMARY KEY,
  area_id TEXT REFERENCES trade_areas(id),
  period TEXT NOT NULL,
  change_indicator TEXT,           -- 'LL', 'LH', 'HL', 'HH'
  survival_avg_months REAL,
  closure_avg_months REAL,
  UNIQUE(area_id, period)
);

-- 집객시설
CREATE TABLE facility_metrics (
  id SERIAL PRIMARY KEY,
  area_id TEXT REFERENCES trade_areas(id),
  period TEXT NOT NULL,
  facility_type TEXT,              -- 'subway', 'university', 'hospital', etc.
  facility_count INTEGER,
  UNIQUE(area_id, period, facility_type)
);
```

**장점**: 확장성, 업종별 분석 가능
**단점**: JOIN 필요, 복잡도 증가

---

## 6. 구현 로드맵

```
┌─────────────────────────────────────────────────────────────────┐
│  Phase 2-A: 보유 데이터 즉시 활용 (1-2일)                         │
│  ├─ 상주인구/직장인구 → resident_index, worker_index            │
│  ├─ 추정매출 → cost_proxy, sales_per_store                      │
│  └─ area_metrics 컬럼 확장 (옵션 A)                              │
├─────────────────────────────────────────────────────────────────┤
│  Phase 2-B: 핵심 3종 다운로드 (2-3일)                            │
│  ├─ 점포-상권 → competition_density, churn                      │
│  ├─ 상권변화지표 → change_indicator (LL/LH/HL/HH)               │
│  └─ 8지표 등급 공식 완성                                         │
├─────────────────────────────────────────────────────────────────┤
│  Phase 2-C: 해석 레이어 (1-2일)                                  │
│  ├─ 집객시설 → 앵커 신호                                         │
│  ├─ 마케팅 탄성 지표                                             │
│  └─ UX 문장 보정 ("광고 영향 높은 상권" 등)                       │
├─────────────────────────────────────────────────────────────────┤
│  Phase 3: Lv4 핀 반경 분석 (1주)                                 │
│  ├─ 소상공인 상가정보 (개별 점포 좌표)                            │
│  ├─ stores 테이블 생성                                           │
│  └─ 반경 집계 쿼리                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 지표별 데이터 매핑 (최종)

| 지표 | 현재 | 데이터 출처 | 계산 방법 | 효과 |
|------|:----:|------------|----------|------|
| `traffic_index` | ✅ | 유동인구.csv | (사용중) | - |
| `daypart_variance` | ✅ | 유동인구.csv | (사용중) | - |
| `weekend_ratio` | ✅ | 유동인구.csv | (사용중) | - |
| `resident_index` | ❌→✅ | 상주인구.csv | `총_상주인구_수 / 10000` | A/B 구분↑ |
| `worker_index` | ❌→✅ | 직장인구.csv | `총_직장_인구_수 / 10000` | A/B 구분↑ |
| `competition_density` | ❌→✅ | 점포-상권 | `유사업종 / 전체점포` | 경쟁 강도 |
| `open_close_churn` | ❌→✅ | 점포-상권 | `(개업+폐업) / 전체` | 변동성 |
| `cost_proxy` | ❌→✅ | 추정매출.csv | `상권매출 / 서울평균` | 비용 압박 |
| `change_indicator` | 신규 | 상권변화지표 | LL/LH/HL/HH | 생존 난이도 |
| `marketing_elasticity` | 신규 | 복합 계산 | HIGH/MED/LOW | 광고 효과 예측 |

---

## 8. 다운로드 체크리스트

### 즉시 다운로드 필요 (서울 열린데이터광장)

- [ ] [점포-상권](https://data.seoul.go.kr/dataList/OA-15577/S/1/datasetView.do) - 경쟁/개폐업
- [ ] [상권변화지표-상권](https://data.seoul.go.kr/dataList/OA-15576/S/1/datasetView.do) - 생존 지표
- [ ] [집객시설-상권](https://data.seoul.go.kr/dataList/OA-15580/S/1/datasetView.do) - 앵커 시설

### Phase 3용 (공공데이터포털)

- [ ] [소상공인 상가(상권)정보](https://www.data.go.kr/data/15083033/fileData.do) - 개별 점포 좌표

---

## 참고 링크

### 서울 열린데이터광장
- [점포-상권](https://data.seoul.go.kr/dataList/OA-15577/S/1/datasetView.do)
- [상권변화지표-상권](https://data.seoul.go.kr/dataList/OA-15576/S/1/datasetView.do)
- [추정매출-상권](https://data.seoul.go.kr/dataList/OA-15572/S/1/datasetView.do)
- [집객시설-상권](https://data.seoul.go.kr/dataList/OA-15580/S/1/datasetView.do)
- [상주인구-상권배후지](https://data.seoul.go.kr/dataList/OA-15569/S/1/datasetView.do)
- [직장인구-상권배후지](https://data.seoul.go.kr/dataList/OA-15570/S/1/datasetView.do)

### 공공데이터포털
- [소상공인 상가(상권)정보 API](https://www.data.go.kr/data/15012005/openapi.do)
- [소상공인 상가(상권)정보 파일](https://www.data.go.kr/data/15083033/fileData.do)

### 서비스 포털
- [서울시 상권분석 서비스 (golmok)](https://golmok.seoul.go.kr/)
- [소상공인365 빅데이터 플랫폼](https://bigdata.sbiz.or.kr/)

---

*작성: OpenRisk 개발팀*
